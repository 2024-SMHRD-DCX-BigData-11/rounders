<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.baseballtalk.database.ManagerMapper">

	<select id="showFreeBoard" resultType = "FreeBoard">
	
		select * from tbl_freeboard
		
	</select>
	
	<select id="showTeamBoard" resultType = "TeamBoard">
	
		select * from tbl_team_board
		
	</select>
	
	<select id="showAllMember" resultType = "Member">
	
		select * from tbl_member
		
	</select>
  	
</mapper>